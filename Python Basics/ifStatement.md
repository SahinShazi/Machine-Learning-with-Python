# Python Conditional Statements - শর্তমূলক বিবৃতি

## 📚 কনসেপ্ট এবং তত্ত্ব

### Conditional Statement কী?

Conditional Statement হলো এমন কোড যা নির্দিষ্ট শর্তের উপর ভিত্তি করে বিভিন্ন কাজ সম্পাদন করে। এটি প্রোগ্রামকে সিদ্ধান্ত নেওয়ার ক্ষমতা দেয়।

### মূল উপাদান:

#### 1. **if Statement**
```python
if শর্ত:
    # শর্ত সত্য হলে এই কোড চলবে
```

#### 2. **elif Statement** (else if)
```python
elif শর্ত:
    # প্রথম শর্ত মিথ্যা কিন্তু এই শর্ত সত্য হলে এই কোড চলবে
```

#### 3. **else Statement**
```python
else:
    # সব শর্ত মিথ্যা হলে এই কোড চলবে
```

### তুলনামূলক অপারেটর (Comparison Operators):

| অপারেটর | অর্থ | উদাহরণ |
|---------|------|--------|
| `==` | সমান | `5 == 5` → True |
| `!=` | সমান নয় | `5 != 3` → True |
| `>` | বড় | `5 > 3` → True |
| `<` | ছোট | `3 < 5` → True |
| `>=` | বড় বা সমান | `5 >= 5` → True |
| `<=` | ছোট বা সমান | `3 <= 5` → True |

### লজিক্যাল অপারেটর (Logical Operators):

| অপারেটর | অর্থ | উদাহরণ |
|---------|------|--------|
| `and` | এবং (উভয় সত্য) | `True and True` → True |
| `or` | অথবা (যেকোনো একটি সত্য) | `True or False` → True |
| `not` | বিপরীত | `not True` → False |

### Modulo Operator (%):
- `%` অপারেটর ভাগশেষ বের করে
- উদাহরণ: `10 % 3 = 1` (10 ÷ 3 = 3 ভাগশেষ 1)
- জোড়/বিজোড় যাচাই: `num % 2 == 0` (জোড় হলে True)

---

## 💻 সমাধানসহ সমস্যা

### Problem 01: সংখ্যা শূন্য, ধনাত্মক নাকি ঋণাত্মক?

**শেখার বিষয়:**
- একাধিক শর্ত পরীক্ষা করা
- `if-elif-else` এর সঠিক ব্যবহার

**কোড:**
```python
num = 0

if num == 0:
    print("Zero")
elif num > 0:
    print("Positive")
else:
    print("Negative")
```

**ব্যাখ্যা:**
1. প্রথমে চেক করি `num == 0` কিনা
2. তারপর চেক করি `num > 0` কিনা (ধনাত্মক)
3. বাকি সব ক্ষেত্রে সংখ্যা ঋণাত্মক

**Output:** `Zero`

---

### Problem 02: জোড় নাকি বিজোড় সংখ্যা?

**শেখার বিষয়:**
- Modulo operator (`%`) এর ব্যবহার
- জোড়/বিজোড় নির্ণয়ের লজিক

**কোড:**
```python
num = 21

if num % 2 == 0:
    print("Even")
else:
    print("Odd")
```

**ব্যাখ্যা:**
- যেকোনো সংখ্যা 2 দিয়ে ভাগ করলে ভাগশেষ 0 হলে জোড়
- অন্যথায় বিজোড়

**Output:** `Odd` (কারণ 21 ÷ 2 = 10 ভাগশেষ 1)

---

### Problem 03: বয়স অনুযায়ী ক্যাটাগরি নির্ণয়

**শেখার বিষয়:**
- একাধিক পরিসীমা (range) পরীক্ষা
- ক্রমিক শর্ত যাচাই

**কোড:**
```python
age = 60

if age < 12:
    print("Child")
elif age < 19:
    print("Teenager")
elif age < 59:
    print("Adult")
else:
    print("Senior Citizen")
```

**ব্যাখ্যা:**
- **0-11:** শিশু (Child)
- **12-18:** কিশোর (Teenager)
- **19-58:** প্রাপ্তবয়স্ক (Adult)
- **59+:** সিনিয়র সিটিজেন

**Output:** `Senior Citizen`

---

### Problem 04: তিনটি সংখ্যার মধ্যে বৃহত্তম

**শেখার বিষয়:**
- `and` অপারেটরের ব্যবহার
- একাধিক শর্ত একসাথে পরীক্ষা

**কোড:**
```python
a = 12
b = 2
c = 19

if a > b and a > c:
    print("a is greatest")
elif b > a and b > c:
    print("b is greatest")
else:
    print("c is greatest")
```

**ব্যাখ্যা:**
- প্রথমে দেখি a বড় কিনা (b এবং c উভয়ের থেকে)
- তারপর দেখি b বড় কিনা
- বাকি থাকলে c-ই বড়

**Output:** `c is greatest` (19 সবচেয়ে বড়)

---

### Problem 05: FizzBuzz Challenge

**শেখার বিষয়:**
- একাধিক শর্ত একসাথে পরীক্ষা
- প্রায়োরিটি অনুযায়ী শর্ত সাজানো

**সমস্যা:**
- 3 এবং 5 উভয় দিয়ে বিভাজ্য → "FizzBuzz"
- শুধু 3 দিয়ে বিভাজ্য → "Fizz"
- শুধু 5 দিয়ে বিভাজ্য → "Buzz"
- অন্যথায় → সংখ্যাটি প্রিন্ট করো

**কোড:**
```python
num = 15

if num % 3 == 0 and num % 5 == 0:
    print("FizzBuzz")
elif num % 3 == 0:
    print("Fizz")
elif num % 5 == 0:
    print("Buzz")
else:
    print(num)
```

**গুরুত্বপূর্ণ নোট:**
- উভয় দিয়ে বিভাজ্যের শর্ত **প্রথমে** রাখতে হবে
- কারণ 15, 3 এবং 5 উভয় দিয়েই বিভাজ্য

**Output:** `FizzBuzz`

---

### Problem 06: আবহাওয়া বিশ্লেষণ

**শেখার বিষয়:**
- একাধিক ভ্যারিয়েবল নিয়ে কাজ
- জটিল শর্ত তৈরি

**সমস্যা:**
- তাপমাত্রা > 30 এবং আর্দ্রতা > 70 → "Hot and humid"
- তাপমাত্রা > 30 কিন্তু আর্দ্রতা < 70 → "Hot but dry"
- তাপমাত্রা < 30 কিন্তু আর্দ্রতা > 70 → "Cool but humid"
- অন্যথায় → "Pleasant weather"

**কোড:**
```python
temperature = 36
humidity = 80

if temperature > 30 and humidity > 70:
    print("Hot and humid")
elif temperature > 30 and humidity < 70:
    print("Hot but dry")
elif temperature < 30 and humidity > 70:
    print("Cool but humid")
else:
    print("Pleasant weather")
```

**ব্যাখ্যা:**
- দুইটি শর্ত একসাথে চেক করা হচ্ছে
- `and` ব্যবহার করে উভয় শর্ত সত্য কিনা যাচাই

**Output:** `Hot and humid`

---

### Problem 07: দুটি সংখ্যার চিহ্ন যাচাই

**শেখার বিষয়:**
- `and` এবং `or` অপারেটর একসাথে
- জটিল লজিক্যাল শর্ত

**সমস্যা:**
- উভয় ধনাত্মক → "Both positive"
- উভয় ঋণাত্মক → "Both negative"
- একটি ধনাত্মক, একটি ঋণাত্মক → "One positive, one negative"

**কোড:**
```python
a = -9
b = -7

if a > 0 and b > 0:
    print("Both positive")
elif a < 0 and b < 0:
    print("Both negative")
elif (a > 0 and b < 0) or (a < 0 and b > 0):
    print("One positive, one negative")
else:
    print("Something unusual happened")
```

**ব্যাখ্যা:**
- প্রথম শর্ত: উভয়ই 0 এর বড়
- দ্বিতীয় শর্ত: উভয়ই 0 এর ছোট
- তৃতীয় শর্ত: একটি বড়, একটি ছোট (যেকোনো ক্রমে)
- শেষ শর্ত: অস্বাভাবিক কেস (যেমন 0)

**Output:** `Both negative`

---

## 🎯 মনে রাখার টিপস

1. **শর্তের ক্রম গুরুত্বপূর্ণ**: সবচেয়ে নির্দিষ্ট শর্ত প্রথমে রাখুন
2. **Indentation**: Python-এ ইন্ডেন্টেশন (spacing) অত্যন্ত জরুরি
3. **== vs =**: `==` তুলনা করে, `=` মান assign করে
4. **and vs or**: 
   - `and` → সব শর্ত সত্য হতে হবে
   - `or` → যেকোনো একটি সত্য হলেই চলবে

## 📝 অনুশীলনের জন্য প্রশ্ন

1. একটি প্রোগ্রাম লিখুন যা পরীক্ষার নম্বর অনুযায়ী গ্রেড দেয় (A, B, C, D, F)
2. একটি বছর লিপ ইয়ার কিনা তা যাচাই করুন
3. তিনটি সংখ্যা ত্রিভুজ তৈরি করতে পারে কিনা চেক করুন

---

**Happy Coding! 🚀**